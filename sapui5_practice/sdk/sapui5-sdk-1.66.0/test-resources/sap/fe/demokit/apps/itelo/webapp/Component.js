/* global hasher */
sap.ui.define([
	"sap/fe/AppComponent"
], function (AppComponent) {
    "use strict";

    return AppComponent.extend("itelo.Component", {
		metadata: {
			"manifest": "json"
		}	
	});
});